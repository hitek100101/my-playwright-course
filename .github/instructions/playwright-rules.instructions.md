# My Project Automation Rules

- Always write element selectors using user-facing roles like page.getByRole().
- Never use hardcoded sleep time statements like page.waitForTimeout(). Always use dynamic waiting.